name: Build and Publish

on:
  workflow_dispatch:

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    steps:
      - name: Test NPM Publish Trigger
        run: |
          echo "🧪 TESTING MODE - NPM publish would run here"
          echo "This workflow was triggered by: ${{ github.event_name }}"
          echo "✅ NPM Publish workflow successfully triggered!"

    # COMMENTED OUT FOR TESTING - Uncomment for production
    # uses: newrelic/video-core-js/.github/workflows/npm-publish.yml@stable
    # with:
    #   s3-path: 'media-agents/browser/html5'
    #   file-to-upload: 'dist/umd/newrelic-video-html5.min.js'
    # secrets:
    #   AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #   AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
